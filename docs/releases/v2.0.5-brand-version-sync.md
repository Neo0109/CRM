# v2.0.5 Brand version sync

- Bumped the visible CRM header label to `Neo's BD Matrix · v2.0.5` so the workbench reflects the latest product iteration.
- Updated the frontend runtime and CSS fallback version sources together to prevent older enhancement modules from rewriting the label back to `v2.0`.
- Updated both local Express and Cloudflare Pages health endpoints to report `v2.0.5-brand-version-sync`.

This release is a product/UI version correction only. It does not change the daily report automation contract or sourcing rule version.
