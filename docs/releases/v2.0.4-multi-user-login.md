# v2.0.4 多人登录

- 新增 `CRM_USERS_JSON`，支持在 Cloudflare 里配置多个 CRM 登录账号。
- 每个账号可预留 `role` 和 `permissions` 字段，方便后续扩展权限和操作记录。
- 保留旧的 `CRM_USERNAME` + `CRM_ACCESS_TOKEN` 单账号配置，避免现有线上登录被打断。
- 登录页和设置页文案更新为多人账号模型。
