# v1.9 Logout Button

- Added a left-sidebar logout button at the bottom of the CRM navigation.
- Logout clears the saved CRM access token from local storage and the browser cookie, then returns the app to the access-token entry state.
- Bumped the visible CRM version and health endpoint to `v1.9`.
