# v1.6.5 Brand version label

## Summary

- Show the header label as `Neo's BD Matrix · v1.6.5`.
- Keep the CSS fallback aligned with the same label, so stale hardcoded text is hidden visually.
- Update `/api/health` to `v1.6.5-brand-version-label`.

## Reason

The CRM needs a stable brand label and a visible version number at the same time, so operators can quickly confirm whether the deployed page is current.
