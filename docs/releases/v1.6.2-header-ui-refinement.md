# v1.6.2 Header UI refinement

## Summary

- Display the current CRM version as `v1.6.2` in the header.
- Hide the Settings entry from the top navigation while there is no active settings workflow.
- Keep Cloudflare-managed password behavior from v1.6.1 unchanged.

## Notes

Settings code is left in place for future reuse, but the top-level entry is hidden to keep the daily CRM workspace cleaner.
