# v2.5 Version Governance Catch-Up

## Scope

- Product-visible version record only.
- No login, sourcing-rule behavior, daily-report schedule, schema, or manual workflow changes.

## Changes

- Bumped the visible CRM product version to `v2.5`.
- Updated health metadata to `v2.5-version-governance-catchup`.
- Clarified that product versions, sourcing-rule versions, and automation-only patch records are separate version lines.
- Documented that future user-facing product changes must use `npm run version:product` instead of relying on sourcing-rule release notes.
- Marked the historical `v2.4.1-source-breakdown-guard` note as an automation patch record, not a product-visible release.

## Verification

- Version constants and health metadata are synchronized with `config/product-version.json`.
- No daily-report workflow, login, schema, or sourcing automation files were changed.
