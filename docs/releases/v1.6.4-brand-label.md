# v1.6.4 Brand label fix

## Summary

- Replace the small header label under the B icon with `Neo's BD Matrix`.
- Add a CSS-level fallback so old hardcoded version text is visually hidden even if it remains in the base component.
- Keep the Settings navigation entry hidden while settings are not part of the active workflow.
- Update `/api/health` to `v1.6.4-brand-label`.

## Reason

The previous runtime-only version correction was not reliable enough. This release adds a stable visual override in the stylesheet and keeps the lightweight runtime correction as a secondary fallback.
