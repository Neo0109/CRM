# v1.8.8 Released Window Guard

- Removed `Popular New` from V4 CRM lead sourcing so already-launched products are not treated as new BD candidates.
- Added an explicit hard-drop guard for Steam rows whose normalized release date is before the report date.
- Updated V3 rules and prompts to state that already released projects cannot enter push/watch candidates; they can only be dropped or used as market background.
- Regenerated the daily report after the rule fix so the new data can be checked against the `未处理` inbox flow.
