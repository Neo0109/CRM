# v2.3.4 Daily Inbox User Display

## Summary

- Reworked daily report sync so today's report can be force-resynced from the UI refresh action.
- When a daily report hits an existing lead that is still only an unreviewed inbox/watch item, the lead returns to `未处理` for human review.
- Preserved active human workflow states such as `待评测`, `测试中`, `跟进中`, `推进池`, and `淘汰池`.
- Added login display names so `nanyuan` shows `南鸢` and `yuyang` shows `于老板`.

## Version

- Visible version: `v2.3.4`
- Health version: `v2.3.4-daily-inbox-user-display`
